import Context, { IContextOptions } from './context';
import { Attachment } from '../attachments';
import { inspectCustomData } from '../../utils/constants';
export interface IGroupUpdateContextPayload {
    user_id: number;
    admin_id: number;
    level_old?: number;
    level_new?: number;
    changes?: Record<string, {
        old_value: string;
        new_value: string;
    }>;
    photo?: object;
}
export declare type GroupUpdateContextOptions<S> = Omit<IContextOptions<IGroupUpdateContextPayload, S>, 'type' | 'subTypes'>;
export default class GroupUpdateContext<S = Record<string, any>> extends Context<IGroupUpdateContextPayload, S> {
    attachments: Attachment[];
    constructor(options: GroupUpdateContextOptions<S>);
    /**
     * Checks is change photo
     */
    readonly isChangePhoto: boolean;
    /**
     * Checks is change officers
     */
    readonly isChangeOfficers: boolean;
    /**
     * Checks is change settings
     */
    readonly isChangeSettings: boolean;
    /**
     * Checks for the presence of attachments
     */
    hasAttachments(type?: string): boolean;
    /**
     * Returns the identifier admin
     */
    readonly adminId: number | null;
    /**
     * Returns the identifier user
     */
    readonly userId: number;
    /**
     * Returns the old level permission
     */
    readonly oldLevel: number | null;
    /**
     * Returns the new level permission
     */
    readonly newLevel: number | null;
    /**
     * Returns the changes settings
     */
    readonly changes: Record<string, {
        old_value: string;
        new_value: string;
    }> | null;
    /**
     * Returns the attachments
     */
    getAttachments(type?: string): Attachment[];
    /**
     * Returns the custom data
     */
    [inspectCustomData](): object;
}
